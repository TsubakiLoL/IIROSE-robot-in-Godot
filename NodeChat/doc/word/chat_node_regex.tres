[gd_resource type="Resource" script_class="SingleWord" load_steps=2 format=3 uid="uid://y8pt1aab7gvq"]

[ext_resource type="Script" path="res://NodeChat/doc/class/single_word.gd" id="1_j8o25"]

[resource]
script = ExtResource("1_j8o25")
name = "正则匹配器"
doc = "用正则匹配器来匹配文本的节点。
[i][color=yellow]输入端口：0：字符串[/color][/i]
[i][color=blue]输出端口：0：布尔类型
输出端口：1：字典类型[/color][/i]
生成预设文本的正则匹配器。
例如 小千(?<信息>.*) 就是匹配小千开头的消息，并把小千后的文本字符串填入到结果字典 “信息”键对应的值中，
如果没有匹配到，则向输出端口0中输出假，并向输出端口1中输出空字典{}，如果匹配到了，则向输出端口0中输出真，并输出结果字典到输出端口1中。"
next_word_array = Array[ExtResource("1_j8o25")]([])
